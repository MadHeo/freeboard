import { MainBox } from "../../../../freeboard_frontend/styles/styles";
import { gql, useMutation } from "@apollo/client";
import { useState } from "react";
import { useRouter } from "next/router";

export default function detailPage() {
  return (
    <div>
      <div>dd</div>
      <MainBox>dd</MainBox>
    </div>
  );
}
